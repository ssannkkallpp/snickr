# Flask secret key — required, no default. Use a long random string in production.
FLASK_SECRET_KEY=change-me-to-a-long-random-string

# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=snickr
DB_USER=postgres
DB_PASSWORD=your_db_password_here
